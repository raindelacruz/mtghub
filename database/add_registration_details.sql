ALTER TABLE users
    ADD COLUMN IF NOT EXISTS first_name VARCHAR(80) NOT NULL DEFAULT '' AFTER username,
    ADD COLUMN IF NOT EXISTS middle_initial VARCHAR(5) NOT NULL DEFAULT '' AFTER first_name,
    ADD COLUMN IF NOT EXISTS last_name VARCHAR(80) NOT NULL DEFAULT '' AFTER middle_initial,
    ADD COLUMN IF NOT EXISTS contact_number VARCHAR(30) NOT NULL DEFAULT '' AFTER email,
    ADD COLUMN IF NOT EXISTS address_number VARCHAR(50) NOT NULL DEFAULT '' AFTER password_hash,
    ADD COLUMN IF NOT EXISTS address_street VARCHAR(150) NOT NULL DEFAULT '' AFTER address_number,
    ADD COLUMN IF NOT EXISTS address_barangay VARCHAR(100) NOT NULL DEFAULT '' AFTER address_street,
    ADD COLUMN IF NOT EXISTS address_province VARCHAR(100) NOT NULL DEFAULT '' AFTER address_barangay,
    ADD COLUMN IF NOT EXISTS address_city VARCHAR(100) NOT NULL DEFAULT '' AFTER address_province,
    ADD COLUMN IF NOT EXISTS address_postal_code VARCHAR(20) NOT NULL DEFAULT '' AFTER address_city,
    ADD COLUMN IF NOT EXISTS shipping_same_as_complete TINYINT(1) NOT NULL DEFAULT 0 AFTER address_postal_code,
    ADD COLUMN IF NOT EXISTS shipping_number VARCHAR(50) NOT NULL DEFAULT '' AFTER shipping_same_as_complete,
    ADD COLUMN IF NOT EXISTS shipping_street VARCHAR(150) NOT NULL DEFAULT '' AFTER shipping_number,
    ADD COLUMN IF NOT EXISTS shipping_barangay VARCHAR(100) NOT NULL DEFAULT '' AFTER shipping_street,
    ADD COLUMN IF NOT EXISTS shipping_province VARCHAR(100) NOT NULL DEFAULT '' AFTER shipping_barangay,
    ADD COLUMN IF NOT EXISTS shipping_city VARCHAR(100) NOT NULL DEFAULT '' AFTER shipping_province,
    ADD COLUMN IF NOT EXISTS shipping_postal_code VARCHAR(20) NOT NULL DEFAULT '' AFTER shipping_city,
    ADD COLUMN IF NOT EXISTS delivery_mode VARCHAR(40) NOT NULL DEFAULT 'meetup' AFTER shipping_postal_code,
    ADD COLUMN IF NOT EXISTS payment_mode VARCHAR(40) NOT NULL DEFAULT 'gcash' AFTER delivery_mode;
