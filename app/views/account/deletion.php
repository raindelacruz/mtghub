<div class="row justify-content-center"><div class="col-lg-7"><div class="archive-card p-4"><p class="text-uppercase fw-semibold archive-kicker-dark mb-1">Privacy</p><h1 class="h3">Delete account</h1><?php if ($request): ?><div class="alert alert-warning">Deletion is scheduled for <strong><?= e($request['scheduled_for']) ?></strong>. Trading is disabled. Completed transaction, wallet, moderation, and tax/audit records are retained in anonymized form for up to seven years.</div><form method="post" action="<?= e(url('/account/deletion/cancel')) ?>"><button class="btn btn-outline-dark">Cancel deletion</button></form><?php else: ?><p>Your account is anonymized after a 30-day cooling-off period. Active orders, open disputes, administrator status, or a non-zero store-credit balance must be resolved first.</p><form method="post" action="<?= e(url('/account/deletion/request')) ?>" onsubmit="return confirm('Schedule permanent account deletion?');"><label class="form-label" for="current_password">Confirm current password</label><input class="form-control" id="current_password" type="password" name="current_password" autocomplete="current-password" required><button class="btn btn-danger mt-3">Schedule account deletion</button></form><?php endif; ?></div></div></div>
